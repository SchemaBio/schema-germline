# =============================================================================
# GLNexus Container
# =============================================================================
# gVCF Joint Genotyping Tool
# https://github.com/dnanexus/GLnexus
#
# TODO: Customize this Dockerfile according to your environment needs
# =============================================================================

# Base image - using official GLNexus release as base
FROM ghcr.io/dnanexus-rnd/glnexus:v1.4.1

# Install additional tools if needed
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     bcftools \
#     tabix \
# && rm -rf /var/lib/apt/lists/*

# Copy custom scripts or configurations
# COPY bin/ /usr/local/bin/

# Default command
# CMD ["glnexus_cli"]
